// @claim rotate4dX turns vectors the same way as GLSL
// @include math/rotate4dX
// @note GLSL and HLSL changed the rotation direction in 9a3a036c (2024); the Metal files kept the old matrices.
results[0] = rotate4dX(1.0) * vec4(0.0, 1.0, 0.0, 1.0);
