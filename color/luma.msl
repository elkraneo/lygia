#include "space/rgb2luma.msl"

/*
contributors: Hugh Kennedy (https://github.com/hughsk)
description: Get the luminosity of a color. From https://github.com/hughsk/glsl-luma/blob/master/index.msl
use: luma(<float3|float4> color)
*/

#ifndef FNC_LUMA
#define FNC_LUMA
static inline float luma(float v) { return v; }
static inline float luma(float3 v) { return rgb2luma(v); }
static inline float luma(float4 v) { return rgb2luma(v.rgb); }
#endif
