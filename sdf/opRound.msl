/*
contributors:  Inigo Quiles
description: round SDFs 
use: <float> opRound( <float> d, <float> h ) 
*/

#ifndef FNC_OPROUND
#define FNC_OPROUND

static inline float opRound( float d, float h ) {
    return d - h;
}

#endif

